const MaxHeap = require("./MaxHeap");
const BinaryTreeNode = require("./BinaryTreeNode");

describe("MaxHeap", () => {

  let maxHeap;
  beforeEach(() => {
    maxHeap = new MaxHeap();
  });

  test("Returns undefined if the tree is empty", () => {});

  describe("extract returns the maximum element", () => {});

});