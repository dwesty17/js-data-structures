const MinHeap = require("./MinHeap");
const BinaryTreeNode = require("./BinaryTreeNode");

describe("MinHeap", () => {

  let minHeap;
  beforeEach(() => {
    minHeap = new MinHeap();
  });

  test("Returns undefined if the tree is empty", () => {});

  describe("extract returns the minimum element", () => {});

});